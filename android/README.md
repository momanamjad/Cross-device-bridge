# Gradle wrapper jar is generated by Android Studio:
# File → New → New Project isn't needed. Open this folder, then
# File → Settings → Build → Gradle, or run:
#   gradle wrapper --gradle-version 8.7
# Android Studio will download the wrapper automatically on first sync.
